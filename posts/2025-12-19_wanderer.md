# Wanderer: A Visual Hash Generator

#hash
#project
#visualisation


Have you ever wondered how the 'randomart' you see when creating an SSH key is generated? Turns out it uses the
brilliantly named [drunken bishop algorithm](https://www.dirk-loss.de/sshvis/drunken_bishop.pdf). Seeing this inspired
me to create my own version: "Wanderer".

Wanderer works by converting each character of the given string into octal (0-7), then mapping each octal character into
a step in a direction. Each of these steps is then followed in order, leaving behind a coloured trail/heatmap of visited
cells.

Enter some text, update the direction-map (hint: you can leave blanks) and click Generate:

```html
<!--[eval class="full-bleed" style="height: 460px;"]-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderer</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: sans-serif;
        }

        .container {
            display: flex;
            gap: 40px;
            align-items: flex-start;
        }

        .canvas-wrapper {
            flex-shrink: 0;
            border: 2px solid #333;
            background-color: #f5f5f5;
            width: 400px;
            aspect-ratio: 1;
        }

        .controls-wrapper {
            flex-shrink: 0;
            display: flex;
            flex-direction: column;
            height: 400px;
            gap: 20px;
        }

        .control-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .control-group label {
            font-size: 14px;
            font-weight: bold;
        }

        .directions-container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            width: 180px;
            aspect-ratio: 1;
        }

        .directions-container > * {
            font-family: monospace;
            font-size: 20px;
            text-align: center;
            padding: 0;
            aspect-ratio: 1;
            width: 100%;
            align-content: center;
        }

        .directions-container > input {
            -moz-appearance: textfield;
            border: 3px solid #8873d3;
            border-radius: 5px;
        }

        .directions-container > input::-webkit-inner-spin-button,
        .directions-container > input::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        #motionText {
            width: 180px;
            padding: 8px;
            font-family: monospace;
            font-size: 14px;
            border: 3px solid #8873d3;
            border-radius: 5px;
            resize: vertical;
            min-height: 60px;
        }

        #runButton {
            width: 180px;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            color: white;
            background-color: #8873d3;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: auto;
        }

        #runButton:hover {
            background-color: #6b5ba8;
        }

        #runButton:active {
            background-color: #5a4a8f;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="canvas-wrapper">
        <img id="canvas" src="" style="display: block; width: 100%; aspect-ratio: 1"/>
    </div>
    <form class="controls-wrapper" id="directionForm">
        <div class="control-group">
            <label>Direction-to-octal map</label>
            <div class="directions-container">
                <input name="NW" type="number" class="grid-item" min="0" max="7" value="0">
                <input name="N" type="number" class="grid-item" min="0" max="7" value="1">
                <input name="NE" type="number" class="grid-item" min="0" max="7" value="2">
                <input name="W" type="number" class="grid-item" min="0" max="7" value="3">
                <div></div>
                <input name="E" type="number" class="grid-item" min="0" max="7" value="4">
                <input name="SW" type="number" class="grid-item" min="0" max="7" value="5">
                <input name="S" type="number" class="grid-item" min="0" max="7" value="6">
                <input name="SE" type="number" class="grid-item" min="0" max="7" value="7">
            </div>
        </div>

        <div class="control-group">
            <label for="motionText">Text</label>
            <textarea id="motionText" required>Move about aimlessly and without any destination</textarea>
        </div>

        <button type="submit" id="runButton">Generate</button>
    </form>
</div>

<script>
    function updateImageSrc(event) {
        event.preventDefault();

        const form = document.getElementById('directionForm');

        if (!form.checkValidity()) {
            form.reportValidity();
            return;
        }

        const params = new URLSearchParams({
            text: document.getElementById('motionText').value,
            directionN: document.querySelector('input[name="N"]').value,
            directionNE: document.querySelector('input[name="NE"]').value,
            directionE: document.querySelector('input[name="E"]').value,
            directionSE: document.querySelector('input[name="SE"]').value,
            directionS: document.querySelector('input[name="S"]').value,
            directionSW: document.querySelector('input[name="SW"]').value,
            directionW: document.querySelector('input[name="W"]').value,
            directionNW: document.querySelector('input[name="NW"]').value,
            noCache: Date.now(),
        });

        document.getElementById('canvas').removeAttribute('src');
        document.getElementById('canvas').setAttribute('src', `/images/wanderer.php?${params}`);
    }

    document
        .getElementById('directionForm')
        .addEventListener('submit', updateImageSrc);

    updateImageSrc(new Event('submit'));
</script>

</body>
</html>
```

If you're curious about how exactly the image is generated and streamed to the browser, you can take a peek at the [source code](https://github.com/moebrowne/mountainofcode.co.uk/blob/master/public/images/wanderer.php)
and have a read of [this post](/php-jpeg-stream).
